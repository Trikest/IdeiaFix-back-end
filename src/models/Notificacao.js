class Notificacao {
    constructor(id, tipo, conteudo, destinatario) {
      this.id = id;
      this.tipo = tipo;
      this.conteudo = conteudo;
      this.destinatario = destinatario;
    }
  
    enviar() {
      // Implementação para enviar notificação
    }
  }
  
  module.exports = Notificacao;
  